{
  "name": "injest-io",
  "version": "0.1.0",
  "private": true,
  "description": "MVP Knowledge Recall System",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "db:migrate": "npm run db:migrate -w packages/api",
    "db:generate": "npm run db:generate -w packages/api"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
